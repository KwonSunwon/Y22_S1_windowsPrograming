#include <iostream>
#include <string>

void main() {
	char input[60];
	char order;

	std::cout << "Input sentenc : ";
	gets_s(input, sizeof(input));

	std::cout << input;

	switch (order)
	{
	case 'e' : 
	default:
		break;
	}
}